name: Test runner

on:
  push:
    branches: [ "main" ]

jobs:
  test:
    runs-on: self-hosted

    steps:
      - name: Test runner
        run: |
          echo "RUNNER OK"
          whoami
          pwd
          node -v
